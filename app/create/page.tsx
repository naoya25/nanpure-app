import { SudokuCreateClient } from "@/components/nanpure/SudokuCreateClient";

export const metadata = {
  title: "盤面作成 | ナンプレトレーニング",
};

export default function CreatePage() {
  return <SudokuCreateClient />;
}
